{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn fastapi_main:app --host 0.0.0.0 --port $PORT --workers 4",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "environments": {
    "production": {
      "variables": {
        "PYTHONPATH": "/app",
        "PYTHONUNBUFFERED": "1",
        "PYTHONDONTWRITEBYTECODE": "1",
        "DEBUG": "false",
        "LOG_LEVEL": "INFO"
      }
    },
    "staging": {
      "variables": {
        "PYTHONPATH": "/app",
        "PYTHONUNBUFFERED": "1", 
        "DEBUG": "true",
        "LOG_LEVEL": "DEBUG"
      }
    }
  }
}